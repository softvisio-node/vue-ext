#!/usr/bin/env node

const fs = require( "fs" );

var json = JSON.parse( fs.readFileSync( "package.json" ) );

delete json.resolutions;

fs.writeFile( "pacckage.json", JSON.stringify( json, null, 4 ) );
